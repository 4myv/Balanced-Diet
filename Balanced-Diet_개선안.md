# Balanced Diet - 개선안

---

## 1. 개선 배경

기획 단계에서부터 "사진 인식"은 완성 이후에 추가할 개선 과제로 예정되어 있었고, 이번 개선안에서 실제로 이 기능을 구현했습니다.

### 기존(1차 버전)의 문제점

| 문제 | 설명 |

| 입력 번거로움 | 먹은 음식의 메뉴명과 양을 매번 텍스트로 직접 타이핑해야 함 |
| 표현의 부담 | "김치찌개 1인분, 공기밥 반 그릇"처럼 양을 말로 옮기는 과정 자체가 귀찮음 |
| 기록 누락 가능성 | 매 끼니 텍스트를 입력하는 게 번거로워서, 실제로는 기록을 건너뛸 가능성이 높음 |

식단 관리 앱이 꾸준히 쓰이려면 기록하는 행위 자체의 마찰이 가장 적어야 하는데, 텍스트 입력 방식은 사용자에게 매번 글을 쓰는 번거로울 수 있는 작업을 요구한다는 한계가 있었습니다.

---

## 2. 개선 내용: 사진으로 식단 등록하기

### 핵심 아이디어

사용자가 먹은 음식을 사진으로 찍어 올리기만 하면, AI가 사진 속 음식을 인식하고 영양 성분(칼로리, 탄수화물, 단백질, 지방)까지 자동으로 추정해서 채워주는 기능을 추가했습니다.

기존 텍스트 입력 기능은 그대로 남겨두고, 텍스트 또는 사진 중 편한 방법을 골라 쓸 수 있도록 두 가지 입력 방식을 병행하는 구조로 설계했습니다.

### Before / After

| 구분 | 기존 (1차 버전) | 개선 후 |

| 입력 방식 | 텍스트 직접 입력만 가능 | 텍스트 입력 또는 사진 업로드 선택 가능 |
| 사용자 행동 | 먹은 메뉴와 양을 문장으로 작성 | 사진 한 장 촬영/선택 |
| AI 입력값 | 사용자가 적은 문장 | 사용자가 올린 이미지 |
| 소요 시간 | 문장을 구성하는 시간 필요 | 촬영 후 버튼 클릭 한 번 |

---

## 3. 기술 구현

### 3-1. 활용 AI 및 방식

이번 개선에서는 Google Gemini API의 멀티모달(이미지 인식) 기능을 활용했습니다. Gemini는 텍스트뿐 아니라 이미지를 함께 입력값으로 받을 수 있어서, 별도의 이미지 인식 모델을 추가로 연동하지 않고도 기존 AI 호출 구조를 그대로 확장할 수 있었습니다.

| 기능 | AI 입력 | AI 출력 |

| (기존) 텍스트 식단 분석 | 사용자가 입력한 음식명·양 | 예상 칼로리 및 탄/단/지 함량 |
| (신규) 사진 식단 분석 | 사용자가 업로드한 음식 사진(base64 인코딩) | 인식된 음식명 + 예상 칼로리 및 탄/단/지 함량 |

### 3-2. 처리 흐름

```
[사용자] 음식 사진 선택/촬영
      ↓
[브라우저] 사진 파일을 base64 문자열로 변환 (FileReader API)
      ↓
[Gemini API] 이미지 데이터 + "영양 성분을 분석해줘" 프롬프트 전송
      ↓
[Gemini API] 음식명과 칼로리·탄·단·지를 JSON 형식으로 응답
      ↓
[브라우저] 응답값을 화면에 표시 → 사용자가 확인 후 저장
```

### 3-3. 핵심 코드 변경 사항

**(1) 사진 업로드 UI 추가**
기존 텍스트 입력창 아래에, 사진을 선택할 수 있는 입력창과 미리보기 이미지를 추가했습니다. 모바일 환경에서는 갤러리 선택뿐 아니라 카메라 촬영도 바로 가능하도록 구성했습니다.

**(2) 사진 → base64 변환 함수 추가**
브라우저가 다루는 사진 파일은 그 자체로 AI에게 보낼 수 없어서, `FileReader`를 이용해 텍스트(base64) 형태로 변환하는 과정을 거칩니다.

**(3) 이미지 인식용 AI 호출 함수 추가**
기존에는 텍스트만 보내는 함수(`callAIForMeal`) 하나뿐이었지만, 이번에 이미지를 함께 보내는 함수(`callAIForMealPhoto`)를 별도로 추가했습니다. 두 함수 모두 같은 형식(JSON)으로 결과를 받기 때문에, 이후 화면에 결과를 표시하는 로직은 그대로 재사용할 수 있었습니다.

**(4) 입력 우선순위 처리**
텍스트와 사진을 동시에 등록할 수 있는 상황을 고려해, **사진이 있으면 사진을 우선 분석**하고 없으면 텍스트를 분석하도록 분기 처리했습니다.

---

## 4. 기대 효과

- **기록 마찰 감소**: 문장을 작성하는 부담이 사라지고, 촬영 한 번으로 기록이 끝나 사용자의 지속 사용 가능성이 높아짐
- **기록 정확도 향상 가능성**: 사용자가 양을 가늠해 텍스트로 옮기는 과정에서 발생하는 오차 대신, 사진 속 실제 음식을 AI가 직접 판단
- **접근성 향상**: 타이핑이 익숙하지 않은 사용자(예: 고령층)도 사진 한 장으로 손쉽게 이용 가능

---

## 5. 한계 및 향후 개선 방향

1차 개선 이후에도 남아있는 한계와, 추가로 발전시킬 수 있는 방향을 정리합니다.

| 한계 | 향후 개선 방향 |

| AI가 첨부된 사진에서 식사량을 정확하게 분석하기 어려움 | 개선 방향 모색 중
| 여러 음식이 한 사진에 섞여 있을 때 인식 정확도가 떨어질 수 있음 | 사진 속 여러 음식을 항목별로 분리해서 각각 분석하는 기능 |
| AI가 추정한 영양 정보가 실제와 다를 수 있음 | 사용자가 결과값을 저장 전에 직접 수정할 수 있는 편집 기능 추가 |
| 매 끼니 사진을 새로 찍어야 함 | 자주 먹는 음식을 "즐겨찾기"로 등록해두고 사진 없이 바로 불러오는 기능 |
| 조명, 각도에 따라 인식률 편차 발생 가능 | 촬영 가이드 안내 문구 추가 |

| 문제 | 향후 개선 방향 |

| AI API 키 노출 가능성 | API 키를 HTTP 리퍼러로 제한하는 방식을 적용해 별도의 서버 없이도 안전하게 배포 가능하도록 구조를 단순화

---

## 6. 추가로 함께 진행된 개선 사항

사진 인식 기능이 이번 개선의 핵심이지만, 프로젝트를 다듬는 과정에서 아래 항목들도 함께 개선했습니다.

- 목표 설정 시 "기타" 항목을 추가해, 사전 정의된 목표(벌크업/다이어트/건강관리) 외의 상황도 직접 입력 가능하도록 확장
- 뒤로가기 기능 추가로 화면 간 이동의 편의성 향상
- 다크 테마 적용으로 눈의 피로도를 줄이고 완성도 있는 UI 구성

---